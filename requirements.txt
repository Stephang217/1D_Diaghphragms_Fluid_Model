# Pinned to the environment the reported results were produced in, so Appendix C
# can quote exact versions and mean it. Developed on Python 3.14.7.
#
# Reproducing the headline result needs numpy and jupyter only. The command in
# Appendix C is
#     jupyter nbconvert --to notebook --execute uq_reproducible_result.ipynb
# which additionally uses pandas and matplotlib to read the cached ensembles and
# draw the figures.
#
# Regenerating the ensembles needs less again: scripts/uq_sweep.py and
# src/model.py import numpy and nothing else.
#
# Loosen to >= on an older Python. Note that the seeded RNG stream is
# not guaranteed identical across numpy major versions, so a regenerated ensemble
# may differ sample-for-sample while matching statistically. The protocol point
# itself is deterministic and reproduces exactly regardless.

numpy==2.3.4
pandas==2.3.3
matplotlib==3.10.7
jupyter

# Only for diaphragm_metafluid.ipynb, which fits a tanh front width with
# scipy.optimize.curve_fit. Nothing else in the repository imports scipy, and it
# is not needed to reproduce any reported number.
scipy==1.16.2
